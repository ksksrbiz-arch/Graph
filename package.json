{
  "name": "graph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal Knowledge Graph — MVP slice",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "ingest:claude-code": "node scripts/ingest-claude-code.mjs",
    "ingest": "node scripts/ingest-claude-code.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
